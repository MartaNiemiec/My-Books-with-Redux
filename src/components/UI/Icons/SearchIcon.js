import styled from 'styled-components';
import { Search } from 'styled-icons/icomoon/Search';

export const SearchIcon = styled(Search)`
        width: 1.2rem;
        height: 1.2rem;
        color: orange;
        `
export const SearchIconBig = styled(Search)`
        width: 3rem;
        height: 3rem;
        color: orange;
        `

// export default searchIcon;
